8_8_2008 is the date on which I burned all the data onto a DVD

look at how revision control was done before I know git. and even worse, at
that time I did not know `diff` either. so I had to use `ad_v2` `ad_v2a`... to
do the code. that looks stupid and made me suffer a lot.
